# Единые переводы строк во всех текстовых файлах (LF).
# Без этого Windows-версия Git конвертирует .env_example в CRLF,
# и на Linux-сервере значения в .env читаются с лишним \r.
* text=auto eol=lf

# Бинарные файлы не трогаем
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
